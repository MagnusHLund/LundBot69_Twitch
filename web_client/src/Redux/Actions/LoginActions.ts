import { baseApiUrl } from '../../Utils/BaseUrl'

export const setTwitchCode = (code: string) => ({
  // TODO: Use thunk to call api and return a session token,
  //       as well as a status for the twitch authentication
})
